# UAE Match — CLAUDE.md

> 本文件是项目的产品与工程决策基线，提炼自《UAE_Match_BRD_v2.1》。每次开发决策与 BRD 冲突时，以 BRD 为准并提醒我。我是本项目唯一的人类（产品负责人 + 运营 + 红娘），所有方案默认按"单人公司"的维护成本来设计。
>
> **本文件是唯一基线正文**：`AGENTS.md` 只是指向本文件的指针，禁止在其中维护第二套规则。
> **AI 协作分工**：Claude Code 为主要开发者（写）；Codex 于阶段完成后做只读复查（产出发现清单，不直接改文件）。详见 §8 与 [docs/glossary.md](docs/glossary.md) §7。
>
> **编号与引用约定见 [docs/glossary.md](docs/glossary.md)**——动手前必读。它规定了裁决号 `DEC-xxx`、冷启动阶段 `P1–P3`、用户状态 `S1–S7` 等命名空间的唯一归属，以及代码注释的引用纪律（六条）。

## 1. 产品一句话

面向**在阿联酋（UAE）以结婚为目标的华人**的 AI 红娘服务。本质不是社交 App，而是**用 AI Agent 把高端红娘工作室的服务做到 1/10 价格**。对标个体红娘，不对标 Tinder/探探。

## 2. 不可动摇的产品决策（写代码前先对齐这些）

1. **稀缺推荐制，没有滑动**：每周 1–3 个推荐（浮动、宁缺毋滥），以"推荐信"形态每周五 20:00 送达。展示顺序**先推荐理由后照片**。用户动作只有三个：愿意认识 / 想再了解（可向红娘追问）/ 这次不合适（强制结构化理由）。72h 响应时限。
2. **道具变现已冻结**：Boost / Super Like / 查看谁喜欢我——代码保留、入口全部下线，不要为它们写任何新代码。收入只来自订阅。
3. **AED 订阅四层**：免费候补（可被推荐、查看回应需付费）/ 标准 149 AED/月 / 高级 199 AED/月 / 尊享 1v1 799 AED/月（限 30 席）。推荐数量对所有付费档一致，高级会员只有排序优先——**稀缺制公平性不可售卖**。
4. **信任三道闸**（进推荐池的前置，缺一不可）：付费墙、AI 深访、EID 核验（BR-107，走持牌 KYC SDK，**平台不收集不存储证件图像**，只存验证结论与最小字段；年龄/性别自核验值锁定不可改）。
5. **北极星指标 = 月度有效配对数**（双方接受推荐且互聊 ≥10 轮），不是 DAU。任何"提升粘性/让人多刷"方向的功能默认是错的。
6. **允许并鼓励交换微信**（BR-406）：双向确认后互显、事件记录、安全提示、红娘事后回访。不要写任何屏蔽/检测微信号的代码。
7. **荣誉毕业**：注销流程前先引导毕业申报（脱单 = 产品成功，回收案例）。

## 3. AI 红娘（核心资产，代号"小缘"，正式名 M1 后定：候选 Vega/Ada/Luna）

- **人设**：婚恋军师（主攻）+ 有使命的陪伴（一切陪伴锚定婚恋旅程，不做通用闲聊 AI）。
- **能力分层即开发顺序**：L1 深访（v1.0，已有雏形需打磨）→ L2 军师（v1.0–v1.2）→ L3 主动性引擎（**v1.0 范围，已提前，不是 v1.5**）→ L4 工具调用代理（v2.0，尊享档专属）。语音消息 v1.5（STT/TTS，**音色必须男/女可选**，默认女声）。
- **记忆架构（BR-202，最高优先技术项）**：三层——事实层（结构化画像）/ 事件层（婚恋旅程时间线）/ 情感层（沟通偏好与情绪模式）。结构化画像库 + 向量记忆库双轨。**现有滚动摘要方案必须废弃重构**。
- **L3 触发源两类**：行为事件（推荐送达/配对/冷场 24h/约会次日回访/被拒 2h 内安抚/**分层沉默唤醒：有未尽事项 3 天轻触达、一般沉默 7 天**）+ 日历事件（中国传统节日、UAE 假日、用户与推荐对象生日、关键日期）。防打扰上限必须内置。
- **访谈三层分离（5.3.5，架构铁律）**：信息目标 Schema（YAML/JSON 独立配置，**禁止埋进 prompt**）/ 问法库（配置化，业务资产）/ 系统 prompt（只放人格与行为原则）。编排层每轮算字段缺口、动态注入问法。
- **深访双产物**：内部画像（含择偶条件弹性系数、心理测评区块、行为元数据）+ 用户报告（五板块，先抽取落库再生成报告，**禁止从对话原文一步直出**）。报告即付费漏斗。
- **心理框架分层（5.3.6）**：匹配算法只用高效度框架（大五、依恋理论、Gottman、价值观相似性；焦虑×回避组合降权、双高神经质降权）。MBTI 只作沟通语言（映射为大五入库）；**星座/属相权重为零，禁止用于否定性表述**。知识注入走硬规则 + 问法库 + RAG，不进 prompt。
- **伦理护栏（BR-206，与 L3 同步上线，不可后置）**：AI 身份透明、依赖检测（日均 >90 分钟或 14 天纯陪伴零推荐互动 → 温和导回真实关系）、危机转介、拒绝暧昧角色扮演。
- **L4 铁规**：读操作自主执行，写操作（改偏好/发推荐/定时间）必须用户口头确认，全留痕。

## 4. AI 成本纪律（5.6，写任何 AI 调用代码时遵守）

- **模型分层路由**：默认 mini 档；深访、推荐分析、复杂咨询升旗舰档。**所有调用走模型抽象层按"任务→档位"路由，业务代码禁止硬编码模型名**（供应商可替换原则）。
- **必开 Prompt Caching**；记忆检索只注入 top-3；兼容性分析/推荐理由按"用户对"缓存。
- **配额**：免费 20 轮/月；语音 100 分钟/月硬上限；4o 升级次数按档位配额。全局月预算熔断（80% 告警、100% 降级 mini）。
- **BR-209 计量埋点与功能同步上线**：每次调用记录 user/scene/model/tokens/cache_hit，M2 起就要有成本看板。成本目标：免费 <1、标准 <2、高级 <8、尊享 <30 AED/月；AI 成本占收入 <5%。

## 5. 技术栈与架构约束

- **形态**：Web + PWA（桌面图标 + 推送）。**不做**：原生 App（v2 再评估）、微信小程序/服务号（微信只是获客渠道）、社区/动态、视频。
- **栈**：沿用现有 Web 代码资产；凡是能买的不自建——IM 用 SaaS、KYC 用 SDK、AI 全走 API、支付用 Stripe（AED）。不自研匹配模型（API + 向量检索）。
- **登录（BR-001, BR-002）**：UAE 手机号 OTP 为主 + 邮箱密码为辅；**不做微信登录、不做 Google/Apple 登录**；默认单设备在线，新登录踢旧会话并通知。
- **i18n（BR-011）**：框架从现在起启用——文案全部抽离语言文件（v1 只有中文包）、Schema/枚举/代码全英文命名、用户中英混杂输入天然支持（红娘中文应答）。法律页面（隐私政策/条款）中英双语。
- **数据合规按 GDPR 级设计**（兼容 PDPL 与 ADGM DPR）：分层同意、敏感字段（收入/签证类型）只参与计算永不展示、注销级联删除（含向量库）、KYC 与 AI 供应商走 DPA。数据 Schema 里不留"以后再删"的口子。
- **性能**：v1 量级（12 个月 ≤2,000 注册）容量不构成约束，**禁止过度设计**。唯一要设计的峰值场景：周五 20:00 推荐信全量送达。

## 6. 当前代码库已知问题（改造时优先处理）

- 支付：商品目录是人民币道具制，需整体重构为 AED 订阅（BR-501, BR-503）。
- 通知系统是桩实现（返回空）。
- 前端存在演示态：注册 10 秒自动过审、VIP 前端开关、mock 兜底——全部要拆除换真实流程。
- 记忆是滚动摘要——废弃，按第 3 节重构。
- 滑动式匹配交互——下线，改推荐信制。

## 7. 开发排期跟随商业里程碑（不按功能清单顺序开发）

- **M2（当前目标，前 50 付费）**：深访打磨 + 记忆重构（BR-201, BR-202）、EID 核验（BR-107, BR-109）、推荐信制（BR-301, BR-302, BR-303, BR-307）、账户体系（BR-001, BR-002, BR-003, BR-004）、AI 计量（BR-209）、Stripe AED 订阅（BR-501, BR-503）。
- **M3**：军师完整化（BR-203, BR-402）、L3 引擎 + 护栏（BR-204, BR-206）、微信交换（BR-406）、见面引导（BR-404）、反馈闭环（BR-304）、调控台（BR-305）。
- **M4**：语音（BR-205）、人格命名定制（BR-208）、徽章扩展（BR-103）、毕业机制（BR-405, BR-504）。
- **M5 / v2**：L4 代理（BR-207）、原生 App 评估、Phase 2（跨族裔 + 英文包）评估。

## 8. 协作方式

- 改动涉及第 2 节任何一条时，先停下来向我确认，不要"顺手优化"。
- 新功能先问"这对月度有效配对数有什么贡献"，答不上来就质疑需求。
- 每个 PR 级别的改动在提交说明里标注对应 BR 编号（书写格式见 glossary R-4：多个编号逗号分隔写全，禁止 `BR-501/503` 式合写）。
- 新增任何编号（字段号/裁决号/层号/阶段号）之前先查 [docs/glossary.md](docs/glossary.md) §2 命名空间表，不得复用已占用的符号族。
- 代码注释只引用 BRD/PRD、裁决号（DEC-xxx）、HLD（写全文件名+章节），**不引用本文件**。
- 交给 Codex 复查之前必须先 commit：未跟踪文件会让接手方丢失全部上下文。
- 我不是专业开发者：解释技术取舍时给我业务语言 + 一个类比；但代码质量按专业标准来，不要因为我看不懂就偷懒。
