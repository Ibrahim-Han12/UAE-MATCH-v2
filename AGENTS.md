# UAE Match — AGENTS.md

> **本项目的基线正文在 [`CLAUDE.md`](CLAUDE.md)**（产品铁律、AI 成本纪律、技术栈约束、里程碑排期）。动手前先完整读它——本文件不重复其内容，也禁止在此维护第二套规则。
> **编号与引用约定在 [`docs/glossary.md`](docs/glossary.md)**（裁决号 `DEC-xxx`、冷启动阶段 `P1–P3`、用户状态 `S1–S7`、六条引用纪律）。新增任何编号前必查其 §2 命名空间表。
> **你的角色（Codex）**：阶段完成后的**只读复查**——产出发现清单，不直接改文件；改动由 Claude Code 执行。复查范围限于可验证事实（撞车编号、悬空引用、代码与文档一致性、对 BRD/PRD 的对账），**不用于**品味与产品判断（对话质感、交互模式选择）。详见 [`docs/glossary.md`](docs/glossary.md) §7。
